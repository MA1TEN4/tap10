function areaCirculo(radio) {
    const PI = Math.PI;
    return PI * radio *radio;
}
console.log("area del circulo:" , areaCirculo(5));
function sumarArray(Numero) {
    let suma = 0;
    for (let num of numeros) {
        suma += num;
    }
    return suma;
}
const lista = [10, 20, 30, 40];
console.log("suma del Array:" , sumarArray(lista));
